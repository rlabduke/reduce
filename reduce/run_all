# First commandline argument is the input directory, seond the output directory
for file in $1*;
do
	name=`basename $file`
	a=10
	reduce_src/reduce -nosym $file > $2$name 2>err
	a=$?
	if [ $a -eq 0 ];then
		echo "$name success"
	else
		echo "$name failed"
	fi
done
